function squareOfWordLength(str) {
    var result = str.length;
    console.log(`The length of words is ${result}`);
    
    var result = str.length * str.length;
    console.log(`The length of square is ${result}`);
 
}
squareOfWordLength("JavaScript");
 

function squareOfWordLength(str) {
    var result = str.length;
    console.log(`The length of words is ${result}`);
    
    var result = str.length * str.length;
    console.log(`The length of square is ${result}`);
 
}
squareOfWordLength("Google Chrome");
 
function squareOfWordLength(str) {
    var result = str.length;
    console.log(`The length of words is ${result}`);
    
    var result = str.length * str.length;
      console.log(`The length of square is ${result}`);
      
 
}
squareOfWordLength("Web Developer Smart");
 
function show() {
    var str = "I am UI Developer";
    var result = str.length;
    console.log(`The length of string is ${result}`);

    var resulto = result/str.split(" ").length;
    console.log(`The string length and divide by total no. words is ${resulto}`);
    
    
    
}
show();